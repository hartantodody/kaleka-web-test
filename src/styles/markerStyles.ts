import { CircleOptionsProps } from "../interfaces/interfaces";

export const defaultColor: CircleOptionsProps = { color: "#478AF7", fillColor: "#D9D9D9", weight: 2 };

const circleStyles = {
  defaultColor,
};

export default circleStyles;
